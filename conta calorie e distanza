// Utilizzo questa funzione per ottenere le calorie bruciate
getBurnedCalories { calories in
    if let calories = calories {
        print("Calorie bruciate: \(calories) kcal")
    } else {
        print("Impossibile ottenere le calorie bruciate")
    }
}



class LocationManager: NSObject, CLLocationManagerDelegate {
    private var locationManager = CLLocationManager()

    override init() {
        super.init()
        locationManager.delegate = self
        locationManager.requestWhenInUseAuthorization()
        locationManager.startUpdatingLocation()
    }

    func locationManager(_ manager: CLLocationManager, didUpdateLocations locations: [CLLocation]) {
        // Calcola la distanza percorsa e il tempo trascorso utilizzando i dati di posizione
        // Aggiorna l'interfaccia utente con queste informazioni
    }
}

// Uso questa classe per tracciare la posizione e ottenere dati sulla distanza e il tempo
let locationManager = LocationManager()
